# Planora (Flattened Next.js App)

Single Next.js app with App Router, Gemini (Google AI Studio) itinerary generation, MongoDB (Mongoose), Tailwind, React Hook Form + Zod.

## Setup
1) Put these files at project root.
2) Create `.env.local` using `.env.example`.
3) `npm install`
4) `npm run dev` → http://localhost:3000
